# Build artifacts
Libs/
*.xcodeproj
*.xcworkspace
DerivedData/
.build/

# Generated UniFFI bindings are regenerated by Scripts/build-rust.sh.
# Kept in git so `xcodegen generate` works on a fresh checkout; delete if you
# prefer to always regenerate before opening the project.
