{
  "name": "kpopmvlyrics",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "CARGO_INCREMENTAL=0 cargo build --manifest-path src-tauri/Cargo.toml --release",
    "tauri": "tauri",
    "package:deb-rpm": "CARGO_INCREMENTAL=0 tauri build --bundles deb,rpm",
    "package:appimage": "scripts/package-appimage.sh",
    "package:tar": "scripts/package-tar.sh",
    "package:flatpak": "scripts/package-flatpak.sh",
    "package:linux": "npm run package:deb-rpm && npm run package:appimage && npm run package:tar && npm run package:flatpak",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "packageManager": "npm@11.15.0"
}
